[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hw2_tests"
version = "0.1.0"
description = "Tests hw2_inbox impl"
readme = "README.md"
requires-python = ">=3.9"

[tool.uv.sources]
hw2_inbox_impl = { workspace = true }
hw2_inbox_api = {workspace = true}