[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hw2_inbox_api"
version = "0.1.0"
description = "Interface package for Gmail client"
readme = "README.md"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["hw2_inbox_api"]
package-dir = {"" = "src"} 