[project]
name = "interface"
version = "0.1.0"
description = "Interface package for Gmail client"
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"] 